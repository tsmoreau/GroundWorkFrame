kind = "web"
previewPath = "/"
title = "Groundwork — Small Business Admin"
version = "1.0.0"
id = "artifacts/groundwork"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22198

[services.development]
run = "npm run dev"

[services.production]
build = [ "npm", "run", "build" ]
publicDir = ".next"
serve = "static"

[services.env]
PORT = "22198"
BASE_PATH = "/"
