kind = "web"
previewPath = "/"
title = "Groundwork — Small Business Admin"
version = "1.0.0"
id = "artifacts/groundwork"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22198

[services.development]
run = "pnpm --filter @workspace/groundwork run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/groundwork", "run", "build" ]
publicDir = "artifacts/groundwork/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22198"
BASE_PATH = "/"
